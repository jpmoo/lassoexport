/**
 * LassoExport — UI placeholder.
 *
 * The plugin runs entirely from index.js (button is registered with
 * showType: 0 so the plugin view is never displayed). This component
 * exists only because AppRegistry.registerComponent requires one.
 *
 * @format
 */

import React from 'react';
import { View } from 'react-native';

function App(): React.JSX.Element {
  return <View />;
}

export default App;
